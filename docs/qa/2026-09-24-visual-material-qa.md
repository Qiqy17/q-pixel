# Q像素 2.0 界面与材质改版核查

日期：2026-09-24。适用版本：2.0.0 build 9。

## 已核查

- `npm test`：12 个测试文件通过；`npm run build`、`node --check web/app.js`、`git diff --check` 通过。
- 默认 `web/test.html` 在浏览器中显示 `PASS`。回归断言区分 MARD 221 个标准色与 1 个透明扩展色；展示图中 H1 白色按不透明处理。
- 专业工作区参数路径下，1440×900 与 820×1180 首页均无横向溢出；作品卡、搜索、排序与四种创建入口可见。820×1180 中打开作品后，四个工作模式均在辅助阅读树中可达。
- 820×1180 中打开“展示图”，选择暖灰石膏后可见纹理背景及保留原色的图案；打开“成品质感 3D”后可见十种烫法、视角选择和“未实物标定参考模型”标识。
- 展示背景新增三张 Poly Haven 素材，来源及许可见 `web/assets/materials/README.md` 和 `docs/material-reference-library.md`。

## 尚需实测

- MARD 5 mm 十类状态的同条件样片、尺寸与 221 色光学测量，完成后才能更新标定状态。
- 目标 iPad 真机触控和离线联测；120×120 与 500×500 图纸的目标设备性能记录；十类状态与实物对照。
- 本轮浏览器交互未取得新版 4K PNG 下载文件，不能将 4K 导出写作本轮已复验；旧版 build 8 曾完成该流程。

本记录描述代码与浏览器核查。发布包签名、安装版资源和 Git 一致性单独核对。
