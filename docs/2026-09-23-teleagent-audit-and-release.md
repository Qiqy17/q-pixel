# Q像素 2.0 接手审计与验收记录

日期：2026-09-23。基线：`2600b6b`；接手时本地 `main` 含 12 个新增提交，最新为 `f9f2971`，远端仍在基线。本文只评价仓库中可核查的改动，不推断具体作者贡献。

## TeleAgent 阶段已落地

| 范围 | 可核查内容 | 接手评价 |
| --- | --- | --- |
| 色卡/库存 | MARD 221 色注册、缺口/替代/扣减纯函数与界面 | 骨架可用；色卡曾与主编辑器重复，锁定层与缺口提示不完整 |
| 拼制 | 四种底板、按色/行/区域导航、进度 | 基本可用；缝线选项在无缝线时可能生成超规格底板 |
| 项目安全 | v2 日志式存储、检查点、草稿恢复横幅 | 比旧版更安全；跨设备冲突仍需真机联测 |
| 导入/重建/去背景 | 可选优化、重建校准、背景选区工具 | 可用的初版；500×500 画布性能尚无基准 |
| 3D/材质 | Three.js 离线包、十种通用烫法、4K 分块导出 | 功能性原型；缺 MARD 实物标定资产，不能称写实成品 |
| 首页与工作台 | 新品牌配色、四入口、文件夹、五阶段 UI | 方向明显改善；平板 3D 拥挤、缺图破图和旧面板堆叠影响使用 |
| 发布 | App 打包、签名和资源核对脚本 | 有验证框架；构建号默认值与版本进度记录不一致 |

总体评价：模块化与开发速度值得保留，基础功能覆盖面大；不足是集成回归、异常路径、触控布局、真实材质验收和“完成”表述不够严谨。不能用单元测试全绿代替用户流程与实物比对。

## 接手需求清单与执行

| 优先级 | 需求 | 当前结果 |
| --- | --- | --- |
| P0 | 色卡单一数据源、缺文件时保护项目 | 已将编辑器色卡统一接入注册表；主页面和测试页均加载该脚本 |
| P0 | 导入优化可跳过、恢复不阻塞 | 快速导入入口与非模态草稿恢复已保留；浏览器回归通过 |
| P0 | 五阶段工具可达、平板布局 | 阶段面板只显示相关工具；空白画布也可选底板；3D 平板改为横向预设、完整预览、滚动控制区 |
| P0 | 分板/导出正确 | 缝线对齐受实体底板尺寸约束；60×40 图纸实测 6 块并导出 6 页 PDF |
| P0 | 4K 导出与取消 | 4096×4096 PNG 实测，取消后显示“导出已取消”；设备能力提示与分块行为一致 |
| P0 | 库存配色保护锁定层 | 已阻止修改锁定顶层，并提示未替换数量；库存扣减显示真实缺口 |
| P1 | 高频去背景交互 | 底图预渲染、遮罩重绘按帧合并，避免每次指针移动重建整张底图 |
| P1 | 视觉细节 | 首页缺失缩略图用品牌占位图，3D 预览增加轻微表面微纹理与接触阴影 |
| P0 | 发布包一致 | 构建脚本默认 build 8，需在本次提交后构建、验签、安装并核对 Git 同步 |

## 验收证据与边界

- Node 纯函数测试与 `node --check web/app.js` 通过；浏览器默认 `/test.html` 显示 PASS。
- 桌面 1440 px、平板 820 px 首页无横向溢出；平板 3D 面板实际截图检查通过。
- 逐板 PDF：60×40、29×29 底板选项、无识别缝线，输出 6 页。
- 3D：30×30 测试图案输出 4096×4096 PNG；取消导出后状态正确。
- 尚未完成：MARD 5 mm 十类样片、221 色实测与真实纹理接入；目标 iPad 真机、500×500 极限性能和十类实物对照。当前必须显示“未标定通用模型”水印。

发布脚本仅证明包完整、签名有效、资源与源码一致，不等于实物级 3D 或所有设备体验验收。
